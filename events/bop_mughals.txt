namespace = mughal_form
country_event = {
	id = mughal_form.1
	title = mughal_form.1_title
	desc = mughal_form.1_desc

	picture = BATTLE_eventPicture
	
	fire_only_once = yes
	major = yes
	
	trigger = {
		is_year = 1500
		NOT = {is_year = 1550}
		NOT = {tag = PER}
		NOT = {tag = MUG}
		NOT = {tag = TUR}
		NOT = {tag = HLR}
		NOT = {exists = MUG}
		#owns Panipat
		2062 = {
			owner = {
				tag = ROOT
			}
		}
	}
	mean_time_to_happen = {months = 300}
	
	option = {
		name = mughal_form.1_a
		change_tag = MUG
		set_government_rank = 3
		define_ruler = {
			name = "Bâbur"
			dynasty = "Timurid"
			adm = 5
			dip = 5
			mil = 6
			fixed = yes
			age = 30
			claim = 100
			culture = uzbehk
			religion = sunni
		}
		define_ruler_to_general = {
			fire = 5
			shock = 5
			manuever = 3
			siege = 1
		}
		kill_heir = yes
		#permanent claims copypasted from vanilla formation decision
		bengal_region = {
			limit = {
				NOT = { owned_by = ROOT }
			}
			add_permanent_claim = MUG
		}
		hindusthan_region = {
			limit = {
				NOT = { owned_by = ROOT }
			}
			add_permanent_claim = MUG
		}
		west_india_region = {
			limit = {
				NOT = { owned_by = ROOT }
			}
			add_permanent_claim = MUG
		}
		deccan_region = {
			limit = {
				NOT = { owned_by = ROOT }
			}
			add_permanent_claim = MUG
		}
		coromandel_region = {
			limit = {
				NOT = { owned_by = ROOT }
			}
			add_permanent_claim = MUG
		}
		if = {
			limit = { NOT = { government = celestial_empire } }
			change_government = iqta
		}
		change_unit_type = indian
		if = {
			limit = { has_custom_ideas = no }
			country_event = { id = ideagroups.1 } #Swap Ideas
		}
		set_country_flag = formed_mughal_empire_flag
		ai_chance = {factor = 100}
	}
	option = {
		name = mughal_form.1_b
		add_stability = -1
		add_years_of_income = -1
		hindusthan_region = {
			add_core = MUG
		}
		2062 = {
			spawn_rebels = {
   	 			type = nationalist_rebels
    				size = 2
			}
		}
		ai_chance = {factor = 0}
	}
}