namespace = cleanup

country_event = { 
	id = cleanup.1
	title = "cleanup.1.t"
	desc = "cleanup.1.d"
	picture = BATTLE_eventPicture
	
	fire_only_once = yes
	hidden = yes
	
	trigger = {
		any_province = {
			culture_group = iberian
			OR = {
				has_province_modifier = por_settle
				has_province_modifier = spa_settle
			}
		}
	}
	
	mean_time_to_happen = {
		days = 1
	}
	
	option = {
		name = "orion_seize.2.a" 
		ai_chance = { factor = 100 }
		every_province
			limit = { 
				culture_group = iberian
			}
			if = {
				limit = { has_province_modifier = por_settle }
				remove_province_modifier = por_settle
			}
			if = {
				limit = { has_province_modifier = spa_settle }
				remove_province_modifier = spa_settle
			}
		}	
	}
} 

