
country_decisions = {
	fra_centralization = {
		major = yes
		potential = {
			tag = FRA
			NOT = { has_country_flag = fra_centralize }
			NOT = { has_country_modifier = fra_centralize }
			government = monarchy
		}
		allow = {
            stability = 1
            legitimacy = 100
	        total_development = 300
		}
		effect = {
            set_country_flag = fra_centralize
			add_prestige = 5
            add_country_modifier = {
				name = "fra_centralize"
				duration = 9125
			}
			if = {
				limit = { AGU = { exists = yes is_subject_of = ROOT is_subject_of_type = fief } }
				inherit = AGU
			}
			if = {
				limit = { LMA = { exists = yes is_subject_of = ROOT is_subject_of_type = fief } }
				inherit = LMA
			}
			if = {
				limit = { LIM = { exists = yes is_subject_of = ROOT is_subject_of_type = fief } }
				inherit = LIM
			}
			if = {
				limit = { PRI = { exists = yes is_subject_of = ROOT is_subject_of_type = fief } }
				inherit = PRI
			}
        }
		ai_will_do = {
			factor = 1000
        }
	}	
	
	edict_of_moulins = {
		major = yes
		potential = {
			tag = FRA
			NOT = { has_country_flag = edict_of_moulins }
			government = monarchy
		}
		allow = {
            stability = 1
            legitimacy = 100
	        NOT = { current_age = age_of_discovery }
		}
		effect = {
            set_country_flag = edict_of_moulins
			change_government = french_monarchy
			add_prestige = 25
			
            add_country_modifier = {
				name = "centralised_state"
				duration = 7300
			}
			custom_tooltip = edict_of_moulins.tooltip
			hidden_effect = {
				every_subject_country = {
					limit = { is_subject_of_type = fief NOT = { num_of_cities = 9 } }
					every_owned_province = {
						add_core = ROOT
						cede_province = ROOT
					}
				}
			}
        }
		ai_will_do = {
			factor = 1000
        }
	}	
	
	plc_reform_confederation = {
		major = yes
		potential = {
			tag = POL
			senior_union_with = LIT
			NOT = { has_country_modifier = union_of_lublin }
		}
		allow = {
            legitimacy = 100
			LIT = { 
				has_opinion = {
					who = ROOT
					value = 200
				}
			}
            NOT = { current_age = age_of_discovery }
		}
		effect = {
			add_country_modifier = {
				name = "union_of_lublin"
				duration = -1
			}
            hidden_effect = {
				3241 = {
					limit = { country_or_non_sovereign_subject_holds = ROOT }
					add_core = ROOT
					remove_core = LIT
				}
				1943 = {
					limit = { country_or_non_sovereign_subject_holds = ROOT }
					add_core = ROOT
					remove_core = LIT
				}
				if = {
					limit = { ZAZ = { OR = { exists = no is_subject_of = LIT } } }
					yedisan_area = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						cede_province = ZAZ
						remove_core = LIT
					}
				}
				if = {
					limit = { VOL = { OR = { exists = no is_subject_of = LIT } } }
					3238 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						add_core = VOL
						cede_province = VOL
						remove_core = LIT
					}
					2962 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						add_core = VOL
						cede_province = VOL
						remove_core = LIT
					}
					3239 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						add_core = VOL
						cede_province = VOL
						remove_core = LIT
					}
				}
				if = {
					limit = { KIE = { OR = { exists = no is_subject_of = LIT } } }
					west_dniepr_area = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
				#		add_core = KIE
						cede_province = KIE
						remove_core = LIT
					}
					sloboda_ukraine_area = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						add_core = KIE
						cede_province = KIE
						remove_core = CHR
						remove_core = LIT
					}
					5670 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						add_core = KIE
						cede_province = KIE
						remove_core = CHR
						remove_core = LIT
					}
					3246 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						add_core = KIE
						cede_province = KIE
						remove_core = CHR
						remove_core = LIT
					}
					290 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						add_core = KIE
						cede_province = KIE
						remove_core = CHR
						remove_core = LIT
					}
					2407 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						add_core = KIE
						cede_province = KIE
						remove_core = CHR
						remove_core = LIT
					}
				}
				if = {
					limit = { CHR = { OR = { exists = no is_subject_of = LIT } } }
					289 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						cede_province = CHR
						remove_core = LIT
					}
					1945 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						cede_province = CHR
						remove_core = LIT
					}
				}
				if = {
					limit = { OKA = { exists = yes is_subject_of = LIT } }
					5667 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						add_core = OKA
						cede_province = OKA
						remove_core = LIT
					}
					5666 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						add_core = OKA
						cede_province = OKA
						remove_core = LIT
					}
				}
				if = {
					limit = { MSL = { exists = yes is_subject_of = LIT } }
					297 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						add_core = MSL
						cede_province = MSL
						remove_core = LIT
					}
				}
				if = {
					limit = { BLV = { exists = yes is_subject_of = LIT } }
					5664 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						add_core = BLV
						cede_province = BLV
						remove_core = LIT
					}
				}
				if = {
					limit = { SMO = { OR = { exists = no is_subject_of = LIT } } }
					smolensk_area = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						cede_province = SMO
						remove_core = LIT
					}
					4243 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						cede_province = SMO
						remove_core = LIT
					}
					4246 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						cede_province = SMO
						remove_core = LIT
					}
					4244 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						cede_province = SMO
						remove_core = LIT
					}
					3270 = {
						limit = { country_or_non_sovereign_subject_holds = ROOT }
						cede_province = SMO
						remove_core = LIT
					}
				}
			}
			3241 = {
				limit = { country_or_non_sovereign_subject_holds = ROOT }
				cede_province = ROOT
			}
			1943 = {
				limit = { country_or_non_sovereign_subject_holds = ROOT }
				cede_province = ROOT
			}
			if = {
				limit = { KIE = { exists = yes OR = { is_subject = no is_subject_of = LIT is_subject_of = ROOT } } }
				create_subject = {
					subject_type = dominion
					subject = KIE
				}
			}
			if = {
				limit = { VOL = { exists = yes OR = { is_subject = no is_subject_of = LIT is_subject_of = ROOT } } }
				create_subject = {
					subject_type = dominion
					subject = VOL
				}
			}
			if = {
				limit = { ZAZ = { exists = yes OR = { is_subject = no is_subject_of = LIT is_subject_of = ROOT } } }
				create_subject = {
					subject_type = dominion
					subject = ZAZ
				}
			}
			if = {
				limit = { CHR = { exists = yes OR = { is_subject = no is_subject_of = LIT is_subject_of = ROOT } } }
				create_subject = {
					subject_type = dominion
					subject = CHR
				}
			}
			if = {
				limit = { SMO = { exists = yes OR = { is_subject = no is_subject_of = LIT is_subject_of = ROOT } } }
				create_subject = {
					subject_type = dominion
					subject = SMO
				}
			}
			if = {
				limit = { BLV = { exists = yes is_subject_of = LIT } }
				create_subject = {
					subject_type = dominion
					subject = BLV
				}
			}
			if = {
				limit = { OKA = { exists = yes is_subject_of = LIT } }
				create_subject = {
					subject_type = dominion
					subject = OKA
				}
			}
			if = {
				limit = { MSL = { exists = yes is_subject_of = LIT } }
				create_subject = {
					subject_type = dominion
					subject = MSL
				}
			}
			if = {
				limit = { VTN = { exists = yes is_subject_of = LIT } }
				create_subject = {
					subject_type = dominion
					subject = VTN
				}
			}
        }
		ai_will_do = {
			factor = 1000
        }
	}
	
	generic_fief_edict = {
		major = yes
		potential = {
			orion_is_major_power = no
			any_subject_country = {
				is_subject_of = ROOT
				is_subject_of_type = fief
			}
			NOT = { has_country_modifier = centralised_state }
		}
		allow = {
            stability = 1
            legitimacy = 100
            OR = { 
	      	 	current_age = age_of_absolutism
	      	 	current_age = age_of_revolutions
	      	 }
		}
		effect = {
            add_country_modifier = {
				name = "centralised_state"
				duration = 7300
			}
			custom_tooltip = integrate_fiefs.tooltip
			hidden_effect = {
				every_subject_country = {
					limit = { is_subject_of_type = fief NOT = { num_of_cities = 9 } }
					every_owned_province = {
						add_core = ROOT
						cede_province = ROOT
					}
				}
			}
        }
		ai_will_do = {
			factor = 1000
        }
	}	
}
