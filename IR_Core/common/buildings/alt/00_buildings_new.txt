#BUIDLINGS OVERHAUL - AMYZONIS
#
# INTERNAL TAG REFERENCE CONVERSION LIST
# Tier 1, 15th Century Buildings
#marketplace
#workshop
#temple - Temple
#barracks
#shipyard
#fort_15th - Castle
#coastal_defence
# Tier 2, 16th Century Buildings
#courthouse
#dock
#regimental_camp
#fort_16th - Keep
#naval_battery
# Tier 3, 17th Century Buildings
#cathedral - Cathedral
#university
#trade_depot
#grand_shipyard
#training_fields
#fort_17th - Bastion
# Tier 4, 18th Century Buildings
#stock_exchange
#counting_house
#town_hall
#drydock
#conscription_center
#fort_18th - Citadel
# Manufactories
#manufactory - Manufactory
#wharf
#weapons
#textile
#plantations - Plantation
#tradecompany
#farm_estate - Farm
#mills - Mill
#furnace
#ramparts
#soldier_households
#impressment_offices
#state_house

#RURAL
manufactory = {
	cost = 200
	time = 60
	modifier = {
		trade_goods_size = 1.0
	}
}
farm_estate = {
	manufactory = {
		grain
		livestock
		ir_rice
		ir_maize
		ir_potatoes
		ir_millet
		ir_apiculture
	}
	onmap = no
	show_separate = yes

	ai_will_do = {
		factor = 1
	}
}
fishery = {
	manufactory = {
		fish
		ir_whales
	}
	#onmap = yes
	
	ai_will_do = {
		factor = 1
	}
}
orchard = {
	manufactory = {
		wine
		ir_fruit
		ir_dates
		ir_olives
	}
	#onmap = yes
	
	ai_will_do = {
		factor = 1
	}
}
pasture = {
	manufactory = {
		wool
		livestock
	}
	#onmap = yes

	ai_will_do = {
		factor = 1
	}
}
plantations = {
	manufactory = {
		spices
		coffee
		cotton
		sugar
		tobacco
		tea
		cocoa
	}
	#onmap = yes

	ai_will_do = {
		factor = 1
	}
}
mine = {
	manufactory = {
		copper
		iron
		salt
		gems
		ir_silver
	}
	#onmap = yes

	ai_will_do = {
		factor = 1
	}
}
quarry = {
	manufactory = {
		coal
		ir_marble
	}
	#onmap = yes

	ai_will_do = {
		factor = 1
	}
}
mills = {
	manufactory = {
		paper
		tropical_wood
		ir_softwood
	}
	#onmap = yes

	ai_will_do = {
		factor = 1
	}
}
lodge = {
	manufactory = {
		ivory
		fur
		incense
	}
	#onmap = yes

	ai_will_do = {
		factor = 1
	}
}	

#URBAN types?


#
fort_15th = {
	cost = 150
	time = 18
	modifier = {
		fort_level = 1
	}

	# THIS = Province, FROM = Builder(not nessecairly owner, can be overlord)
	on_built = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_nobles
					share = 1
				}
			}
		}
	}

#	# THIS = Province, FROM = Destroyer, Will not be called on buildings made obsolete!
	on_destroyed = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_nobles
					share = -1
				}
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = -5
				}
			}
		}
	}
	
	onmap = yes
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

fort_16th = {
	cost = 300
	time = 36
	make_obsolete = fort_15th
	modifier = {
		fort_level = 2
	}

# THIS = Province, FROM = Builder(not nessecairly owner, can be overlord)
	on_built = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_nobles
					share = 2
				}
			}
		}
	}

#	# THIS = Province, FROM = Destroyer, Will not be called on buildings made obsolete!
	on_destroyed = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_nobles
					share = -2
				}
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = -10
				}
			}
		}
	}

	onmap = yes
	
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

fort_17th = {
	cost = 600
	time = 72
	
	make_obsolete = fort_16th
	
	modifier = {
		fort_level = 3
	}

# THIS = Province, FROM = Builder(not nessecairly owner, can be overlord)
	on_built = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_nobles
					share = 3
				}
			}
		}
	}

#	# THIS = Province, FROM = Destroyer, Will not be called on buildings made obsolete!
	on_destroyed = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_nobles
					share = -3
				}
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = -15
				}
			}
		}
	}

	onmap = yes
	
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

fort_18th = {
	cost = 1200
	time = 144	
	make_obsolete = fort_17th
	modifier = {
		fort_level = 4
	}

# THIS = Province, FROM = Builder(not nessecairly owner, can be overlord)
	on_built = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_nobles
					share = 4
				}
			}
		}
	}

#	# THIS = Province, FROM = Destroyer, Will not be called on buildings made obsolete!
	on_destroyed = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_nobles
					share = -4
				}
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = -20
				}
			}
		}
	}

	onmap = yes
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

#
shrine = {
	cost = 150
	time = 18
	modifier = {
		local_missionary_strength  = 0.01
		local_tax_modifier = 0.05
		local_governing_cost = -0.01
	}

	# THIS = Province, FROM = Builder(not nessecairly owner, can be overlord)
	on_built = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_church
					share = 1
				}
			}
		}
	}

#	# THIS = Province, FROM = Destroyer, Will not be called on buildings made obsolete!
	on_destroyed = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_church
					share = -1
				}
				add_estate_loyalty = {
					estate = estate_church
					loyalty = -5
				}
			}
		}
	}
		
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

monastery = {
	cost = 300
	time = 36	
	make_obsolete = shrine
	modifier = {
		local_missionary_strength  = 0.02
		local_tax_modifier = 0.1
		local_governing_cost = -0.02
	}

# THIS = Province, FROM = Builder(not nessecairly owner, can be overlord)
	on_built = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_church
					share = 2
				}
			}
		}
	}

#	# THIS = Province, FROM = Destroyer, Will not be called on buildings made obsolete!
	on_destroyed = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_church
					share = -2
				}
				add_estate_loyalty = {
					estate = estate_church
					loyalty = -10
				}
			}
		}
	}

	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

temple = {
	cost = 600
	time = 72	
	make_obsolete = monastery	
	modifier = {
		local_missionary_strength  = 0.03
		local_tax_modifier = 0.15
		local_governing_cost = -0.03
	}

# THIS = Province, FROM = Builder(not nessecairly owner, can be overlord)
	on_built = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_church
					share = 3
				}
			}
		}
	}

#	# THIS = Province, FROM = Destroyer, Will not be called on buildings made obsolete!
	on_destroyed = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_church
					share = -3
				}
				add_estate_loyalty = {
					estate = estate_church
					loyalty = -15
				}
			}
		}
	}

	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

cathedral = {
	cost = 1200
	time = 144	
	make_obsolete = temple	
	modifier = {
		local_missionary_strength  = 0.05
		local_tax_modifier = 0.25
		local_governing_cost = -0.05
	}

# THIS = Province, FROM = Builder(not nessecairly owner, can be overlord)
	on_built = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_church
					share = 4
				}
			}
		}
	}

#	# THIS = Province, FROM = Destroyer, Will not be called on buildings made obsolete!
	on_destroyed = {
		THIS = { 
			owner = { 
				change_estate_land_share = {
					estate = estate_church
					share = -4
				}
				add_estate_loyalty = {
					estate = estate_church
					loyalty = -20
				}
			}
		}
	}
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

# URBAN
village = {
	cost = 25               # Cost of the building in ducats
    time = 12             # Time (in months) that it takes to construct the building
  
    indestructible = yes            # Optional, exempts this building from being destroyed via bankruptcy
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		base_manpower = 5
    }
	
	keep_trigger = {
		base_manpower = 5
	}
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
        local_development_cost = 0.05
		supply_limit_modifier = 0.05
		local_institution_spread = 0.05
		allowed_num_of_buildings = 2
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 9999
		modifier = {
			factor = 100 
			base_manpower = 5 
		}
		modifier = {
			factor = 0 
			NOT = { base_manpower = 5 }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_peasants
				#	share = 2
				#}
				#change_estate_land_share = {
				#	estate = estate_citizens
				#	share = 1
				#}
			}
		}
    }
    
#	# THIS = Province, FROM = Destroyer, Will not be called on buildings made obsolete!
	on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_peasants
				#	share = -2
				#}
				#add_estate_loyalty = {
				#	estate = estate_church
				#	loyalty = -5
				#}
				#change_estate_land_share = {
				#	estate = estate_citizens
				#	share = -1
				#}
				#add_estate_loyalty = {
				#	estate = estate_church
				#	loyalty = -5
				#}
			}
		}
	}
 
}

town = {
	cost = 50               # Cost of the building in ducats
    time = 24             # Time (in months) that it takes to construct the building
    make_obsolete = village  # Optional, makes listed building obsolete when this building is constructed
  
    indestructible = yes            # Optional, exempts this building from being destroyed via bankruptcy
   
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		base_manpower = 10
    }
	
	keep_trigger = {
		base_manpower = 10
	}
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
        local_development_cost = 0.1
		supply_limit_modifier = 0.1
		local_institution_spread = 0.1
		allowed_num_of_buildings = 3
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 9999
		modifier = {
			factor = 100 
			base_manpower = 10
		}
		modifier = {
			factor = 0 
			NOT = { base_manpower = 10 }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_peasants
				#	share = 3
				#}
				#change_estate_land_share = {
				#	estate = estate_citizens
				#	share = 3
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
	on_destroyed = {
		if = { 
			limit = { THIS = { base_manpower = 5 } }
			add_building = village
		}
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_peasants
				#	share = -3
				#}
				#add_estate_loyalty = {
				#	estate = estate_church
				#	loyalty = -10
				#}
				#change_estate_land_share = {
				#	estate = estate_citizens
				#	share = -3
				#}
				#add_estate_loyalty = {
				#	estate = estate_church
				#	loyalty = -10
				#}
			}
		}
    }
     
}

city = {
	cost = 100               # Cost of the building in ducats
    time = 48 # Time (in months) that it takes to construct the building
    make_obsolete = town  # Optional, makes listed building obsolete when this building is constructed
  
    indestructible = yes            # Optional, exempts this building from being destroyed via bankruptcy
  
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		base_manpower = 25
    }
	
	keep_trigger = {
		base_manpower = 25
	}
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
        local_development_cost = 0.15
		supply_limit_modifier = 0.2
		local_institution_spread = 0.2
		allowed_num_of_buildings = 4
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 9999
		modifier = {
			factor = 100 
			base_manpower = 25
		}
		modifier = {
			factor = 0 
			NOT = { base_manpower = 25 }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_peasants
				#	share = 2
				#}
				#change_estate_land_share = {
				#	estate = estate_citizens
				#	share = 5
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		if = { 
			limit = { THIS = { base_manpower = 10 } }
			add_building = town
		}
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_peasants
				#	share = -2
				#}
				#add_estate_loyalty = {
				#	estate = estate_church
				#	loyalty = -5
				#}
				#change_estate_land_share = {
				#	estate = estate_citizens
				#	share = -5
				#}
				#add_estate_loyalty = {
				#	estate = estate_church
				#	loyalty = -20
				#}
			}
		}
    }
}

metropolis = { 
	cost = 200               # Cost of the building in ducats
    time = 60           # Time (in months) that it takes to construct the building
    make_obsolete = city  # Optional, makes listed building obsolete when this building is constructed
  
	indestructible = yes            # Optional, exempts this building from being destroyed via bankruptcy
 
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		base_manpower = 50
    }
	
	keep_trigger = {
		base_manpower = 50
	}
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
        local_development_cost = 0.2
		supply_limit_modifier = 0.3
		local_institution_spread = 0.3
		allowed_num_of_buildings = 5
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 9999 
		modifier = {
			factor = 100 
			base_manpower = 50 
		}
		modifier = {
			factor = 0 
			NOT = { base_manpower = 50 }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_citizens
				#	share = 10
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		if = { 
			limit = { THIS = { base_manpower = 25 } }
			add_building = city
		}
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_citizens
				#	share = -10
				#}
				#add_estate_loyalty = {
				#	estate = estate_church
				#	loyalty = -25
				#}
			}
		}
    }
}

#
marketplace = {
	cost = 100               # Cost of the building in ducats
    time = 12             # Time (in months) that it takes to construct the building
   # make_obsolete = <building>  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 1 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		province_trade_power_value = 1
		trade_value_modifier = 0.05
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis province_has_center_of_trade_of_level = 1 } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = 1
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = -1
				#}
				#add_estate_loyalty = {
				#	estate = estate_burghers
				#	loyalty = -5
				#}
			}
		}
    }
}
trade_depot = {
	cost = 250               # Cost of the building in ducats
    time = 24             # Time (in months) that it takes to construct the building
    make_obsolete = marketplace  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 2 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		province_trade_power_value = 2.5
		trade_value_modifier = 0.1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis province_has_center_of_trade_of_level = 2 } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = 2
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = -2
				#}
				#add_estate_loyalty = {
				#	estate = estate_burghers
				#	loyalty = -10
				#}
			}
		}
    }
}
counting_house = {
	cost = 500               # Cost of the building in ducats
    time = 36             # Time (in months) that it takes to construct the building
    make_obsolete = trade_depot  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 3 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		province_trade_power_value = 5
		trade_value_modifier = 0.15
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis province_has_center_of_trade_of_level = 3 } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = 2
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = -2
				#}
				#add_estate_loyalty = {
				#	estate = estate_burghers
				#	loyalty = -10
				#}
			}
		}
    }
}
bank = {
	cost = 1000                 # Cost of the building in ducats
    time = 60             # Time (in months) that it takes to construct the building
    make_obsolete = counting_house  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 4 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		province_trade_power_value = 10
		trade_value_modifier = 0.2
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis province_has_center_of_trade_of_level = 4 } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = 4
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = -4
				#}
				#add_estate_loyalty = {
				#	estate = estate_burghers
				#	loyalty = -20
				#}
			}
		}
    }
}

jetty = {
	cost = 100               # Cost of the building in ducats
    time = 12             # Time (in months) that it takes to construct the building
   # make_obsolete = <building>  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		has_port = yes
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 1 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_sailors_modifier  = 0.25
		province_trade_power_modifier = 0.05
		naval_forcelimit = 1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis province_has_center_of_trade_of_level = 1 } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = 1
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = -1
				#}
				#add_estate_loyalty = {
				#	estate = estate_burghers
				#	loyalty = -5
				#}
			}
		}
    }
}
wharf = {
	cost = 250               # Cost of the building in ducats
    time = 24             # Time (in months) that it takes to construct the building
    make_obsolete = jetty  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		has_port = yes
		OR = { 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 2 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_sailors_modifier  = 0.5
		province_trade_power_modifier = 0.1
		naval_forcelimit = 2
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis province_has_center_of_trade_of_level = 2 } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = 2
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = -2
				#}
				#add_estate_loyalty = {
				#	estate = estate_burghers
				#	loyalty = -10
				#}
			}
		}
    }
}
dockyard = {
	cost = 500               # Cost of the building in ducats
    time = 36             # Time (in months) that it takes to construct the building
    make_obsolete = wharf  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		has_port = yes
		OR = { 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 3 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_sailors_modifier  = 0.75
		province_trade_power_modifier = 0.25
		naval_forcelimit = 3
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis province_has_center_of_trade_of_level = 3 } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = 2
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = -2
				#}
				#add_estate_loyalty = {
				#	estate = estate_burghers
				#	loyalty = -10
				#}
			}
		}
    }
}
harbor = {
	cost = 1000               # Cost of the building in ducats
    time = 60             # Time (in months) that it takes to construct the building
    make_obsolete = dockyard  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		has_port = yes
		OR = { 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 4 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_sailors_modifier  = 1
		province_trade_power_modifier = 0.5
		naval_forcelimit = 5
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis province_has_center_of_trade_of_level = 4 } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = 4
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = -4
				#}
				#add_estate_loyalty = {
				#	estate = estate_burghers
				#	loyalty = -20
				#}
			}
		}
    }
}

#
school = {
	cost = 200               # Cost of the building in ducats
    time = 24             # Time (in months) that it takes to construct the building
   # make_obsolete = <building>  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_institution_spread = 0.05
		innovativeness_gain = 0.005
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_scholars
				#	share = 1
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_scholars
				#	share = -1
				#}
				#add_estate_loyalty = {
				#	estate = estate_scholars
				#	loyalty = -5
				#}
			}
		}
    }
}
college = {
	cost = 500               # Cost of the building in ducats
    time = 48             # Time (in months) that it takes to construct the building
    make_obsolete = school  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = town 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_institution_spread = 0.1
		innovativeness_gain = 0.01
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis  } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_scholars
				#	share = 2
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_scholars
				#	share = -2
				#}
				#add_estate_loyalty = {
				#	estate = estate_scholars
				#	loyalty = -10
				#}
			}
		}
    }
}
university = {
	cost = 1000               # Cost of the building in ducats
    time = 72             # Time (in months) that it takes to construct the building
    make_obsolete = college  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_institution_spread = 0.25
		innovativeness_gain = 0.025
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_scholars
				#	share = 2
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_scholars
				#	share = -2
				#}
				#add_estate_loyalty = {
				#	estate = estate_scholars
				#	loyalty = -10
				#}
			}
		}
    }
}
academy = {
	cost = 2000               # Cost of the building in ducats
    time = 120             # Time (in months) that it takes to construct the building
    make_obsolete = university  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_institution_spread = 0.5
		innovativeness_gain = 0.05
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_scholars
				#	share = 4
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_scholars
				#	share = -4
				#}
				#add_estate_loyalty = {
				#	estate = estate_scholars
				#	loyalty = -20
				#}
			}
		}
    }
}

#
cottage = {
	cost = 100               # Cost of the building in ducats
    time = 12             # Time (in months) that it takes to construct the building
   # make_obsolete = <building>  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_goods_size = 0.5
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = 1
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = -1
				#}
				#add_estate_loyalty = {
				#	estate = estate_artisans
				#	loyalty = -5
				#}
			}
		}
    }
}
workshop = {
	cost = 250               # Cost of the building in ducats
    time = 24             # Time (in months) that it takes to construct the building
    make_obsolete = cottage  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = town 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_goods_size = 1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis  } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = 2
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = -2
				#}
				#add_estate_loyalty = {
				#	estate = estate_artisans
				#	loyalty = -10
				#}
			}
		}
    }
}
new_manufactory = {
	cost = 500               # Cost of the building in ducats
    time = 36             # Time (in months) that it takes to construct the building
    make_obsolete = workshop  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_goods_size = 2
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = 2
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = -2
				#}
				#add_estate_loyalty = {
				#	estate = estate_artisans
				#	loyalty = -10
				#}
			}
		}
    }
}
factory = {
	cost = 1000               # Cost of the building in ducats
    time = 60             # Time (in months) that it takes to construct the building
    make_obsolete = new_manufactory  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_goods_size = 3
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = 4
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = -4
				#}
				#add_estate_loyalty = {
				#	estate = estate_artisans
				#	loyalty = -20
				#}
			}
		}
    }
}

studio = {
	cost = 100               # Cost of the building in ducats
    time = 12             # Time (in months) that it takes to construct the building
   # make_obsolete = <building>  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_value_modifier = 0.05
		monthly_splendor = 0.5
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = 1
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = -1
				#}
				#add_estate_loyalty = {
				#	estate = estate_artisans
				#	loyalty = -5
				#}
			}
		}
    }
}
artisanate = {
	cost = 250               # Cost of the building in ducats
    time = 24             # Time (in months) that it takes to construct the building
    make_obsolete = artisanate  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = town 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_value_modifier = 0.1
		monthly_splendor = 1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis  } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = 2
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = -2
				#}
				#add_estate_loyalty = {
				#	estate = estate_artisans
				#	loyalty = -10
				#}
			}
		}
    }
}
theatre = {
	cost = 500               # Cost of the building in ducats
    time = 36             # Time (in months) that it takes to construct the building
    make_obsolete = artisanate  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_value_modifier = 0.25
		monthly_splendor = 2.5
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = 2
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = -2
				#}
				#add_estate_loyalty = {
				#	estate = estate_artisans
				#	loyalty = -10
				#}
			}
		}
    }
}
museum = {
	cost = 1000               # Cost of the building in ducats
    time = 60             # Time (in months) that it takes to construct the building
    make_obsolete = theatre  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_value_modifier = 0.5
		monthly_splendor = 5
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = 4
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_artisans
				#	share = -4
				#}
				#add_estate_loyalty = {
				#	estate = estate_artisans
				#	loyalty = -20
				#}
			}
		}
    }
}

#ELSE
palisade = {
	cost = 150
	time = 6
	modifier = {
		fort_level = 1
	}
	
	onmap = yes
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
walls = {
	cost = 300
	time = 18
	make_obsolete = palisade
	modifier = {
		fort_level = 2
	}
	
	onmap = yes
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
battlements = {
	cost = 150
	time = 60
	make_obsolete = walls
	modifier = {
		fort_level = 3
	}
	
	onmap = yes
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
entrenchments = {
	cost = 150
	time = 120
	make_obsolete = battlements
	modifier = {
		fort_level = 4
	}
	
	onmap = yes
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

watchtowers = {
	cost = 250
	time = 6
	modifier = {
		local_defensiveness = 0.1
		local_hostile_movement_speed = -0.15
	}
	build_trigger = {
		OR = { 
			has_building = palisade 
			has_building = walls 
			has_building = battlements
			has_building = entrenchments
		}
    }
	keep_trigger = {
		OR = { 
			has_building = palisade 
			has_building = walls 
			has_building = battlements
			has_building = entrenchments
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
barbican = {
	cost = 500
	time = 18
	modifier = {
		local_defensiveness = 0.2
		garrison_growth = 0.25
	}
	build_trigger = {
		OR = { 
		#	has_building = palisade 
			has_building = walls 
			has_building = battlements
			has_building = entrenchments
		}
    }
	keep_trigger = {
		OR = { 
		#	has_building = palisade 
			has_building = walls 
			has_building = battlements
			has_building = entrenchments
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
ravelins = {
	cost = 750
	time = 60
	modifier = {
		local_defensiveness = 0.5
		local_hostile_attrition = 2
	}
	build_trigger = {
		OR = { 
		#	has_building = palisade 
		#	has_building = walls 
			has_building = battlements
			has_building = entrenchments
		}
    }
	keep_trigger = {
		OR = { 
		#	has_building = palisade 
		#	has_building = walls 
			has_building = battlements
			has_building = entrenchments
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
battery = {
	cost = 1000
	time = 120
	modifier = {
		local_hostile_attrition = 1	
		hostile_fleet_attrition = 2
		blockade_force_required = 1
		hostile_disembark_speed = 2
	}
	build_trigger = {
		OR = { 
			#has_building = palisade 
			#has_building = walls 
			has_building = battlements
			has_building = entrenchments
		}
    }
	keep_trigger = {
		OR = { 
			#has_building = palisade 
			#has_building = walls 
			has_building = battlements
			has_building = entrenchments
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

#
encampment = {
	cost = 250
	time = 6
	modifier = {
		local_manpower_modifier  = 0.25
		land_forcelimit = 1
		local_regiment_cost = -0.05
		regiment_recruit_speed = -0.1
	}
	build_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
barracks = {
	cost = 500
	time = 18
	make_obsolete = encampment
	modifier = {
		local_manpower_modifier  = 0.5
		land_forcelimit = 2.5
		local_regiment_cost = -0.1
		regiment_recruit_speed = -0.25
	}
	build_trigger = {
		OR = { 
			#has_building = village 
			has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		OR = { 
			#has_building = village 
			has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
armory = {
	cost = 750
	time = 60
	make_obsolete = barracks
	modifier = {
		local_manpower_modifier = 0.75
		land_forcelimit = 5
		local_regiment_cost = -0.25
		regiment_recruit_speed = -0.5
	}
	build_trigger = {
		OR = { 
			#has_building = village 
			#has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		OR = { 
			#has_building = village 
			#has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
magazine = {
	cost = 1000
	time = 120
	make_obsolete = armory
	modifier = {
		local_manpower_modifier = 1
		land_forcelimit = 10
		local_regiment_cost = -0.5
		regiment_recruit_speed = -0.75
	}
	build_trigger = {
		OR = { 
			#has_building = village 
			#has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		OR = { 
			#has_building = village 
			#has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

shipwright = {
	cost = 250
	time = 6
	modifier = {
		local_sailors_modifier  = 0.25
		local_ship_repair = 0.05
		local_ship_cost = -0.05
		ship_recruit_speed = -0.1
	}
	build_trigger = {
		has_port = yes
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
shipyard = {
	cost = 500
	time = 18
	make_obsolete = shipwright
	modifier = {
		local_manpower_modifier  = 0.5
		local_ship_repair = 0.1
		local_regiment_cost = -0.1
		ship_recruit_speed = -0.25
	}
	build_trigger = {
		has_port = yes
		OR = { 
			#has_building = village 
			has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		OR = { 
			#has_building = village 
			has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
drydock = {
	cost = 750
	time = 60
	make_obsolete = shipyard
	modifier = {
		local_manpower_modifier = 0.75
		local_ship_repair = 0.25
		local_regiment_cost = -0.25
		ship_recruit_speed = -0.5
	}
	build_trigger = {
		has_port = yes
		OR = { 
			#has_building = village 
			#has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		OR = { 
			#has_building = village 
			#has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
arsenal = {
	cost = 1000
	time = 120
	make_obsolete = drydock
	modifier = {
		local_manpower_modifier = 1
		local_ship_repair = 0.5
		local_regiment_cost = -0.5
		ship_recruit_speed = -0.75
	}
	build_trigger = {
		has_port = yes
		OR = { 
			#has_building = village 
			#has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		OR = { 
			#has_building = village 
			#has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

constabulary = {
	cost = 250               # Cost of the building in ducats
    time = 12           # Time (in months) that it takes to construct the building
	modifier = {
		local_autonomy = -0.1
		local_state_maintenance_modifier = -0.1
		local_unrest = -5
	}
	build_trigger = {
		OR = { has_building = village has_building = town has_building = city has_building = metropolis }
		NOT = { area_for_scope_province = { has_building = constabulary } }
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	}
}
courthouse = {
	cost = 500               # Cost of the building in ducats
    time = 30           # Time (in months) that it takes to construct the building
	modifier = {
		local_autonomy = -0.1
		local_state_maintenance_modifier = -0.1
		local_unrest = -5
	}
	build_trigger = {
		OR = { has_building = town has_building = city has_building = metropolis }
		NOT = { area_for_scope_province = { has_building = courthouse } }
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	}
}
prison = {
	cost = 1000               # Cost of the building in ducats
    time = 60           # Time (in months) that it takes to construct the building
	modifier = {
		statewide_governing_cost = -0.1
		local_state_maintenance_modifier = 0.05
		local_unrest = -10
	}
	build_trigger = {
		OR = { has_building = city has_building = metropolis }
		NOT = { area_for_scope_province = { has_building = prison } }
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	}
}

postmaster_general = { 
	cost = 1000               # Cost of the building in ducats
    time = 120           # Time (in months) that it takes to construct the building
   # make_obsolete = barracks  # Optional, makes listed building obsolete when this building is constructed
 
   one_per_country = yes
   indestructible = yes             # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = city has_building = metropolis }
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_range_modifier = 0.5
		envoy_travel_time = -0.5	
		administrative_efficiency = 0.05
		global_institution_spread = 0.1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
  #  }
}

national_court = {
	cost = 1000               # Cost of the building in ducats
    time = 120           # Time (in months) that it takes to construct the building
   # make_obsolete = <building>  # Optional, makes listed building obsolete when this building is constructed
  
   one_per_country = yes
   indestructible = yes             # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = city has_building = metropolis } 
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		global_unrest = -2
		governing_capacity = 250
		num_accepted_cultures  = 2
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
	#}
}

national_mint = {
	cost = 1000              # Cost of the building in ducats
    time = 120        # Time (in months) that it takes to construct the building
   # make_obsolete = training_fields  # Optional, makes listed building obsolete when this building is constructed
  
   one_per_country = yes
   indestructible = yes       # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = city has_building = metropolis }
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		production_efficiency = 0.1
		inflation_action_cost = -0.5	 
		#inflation_reduction = -0.1
		inflation_reduction = 0.05
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
  #  }
}

foreign_office = { 
	cost = 1000               # Cost of the building in ducats
    time = 120           # Time (in months) that it takes to construct the building
    #make_obsolete = regimental_camp  # Optional, makes listed building obsolete when this building is constructed
 
   one_per_country = yes
   indestructible = yes            # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = city has_building = metropolis } 
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		diplomatic_reputation = 1
		diplomatic_upkeep = 1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
  #  }
}

war_college = {
	cost = 1500              # Cost of the building in ducats
    time = 180             # Time (in months) that it takes to construct the building
  #  make_obsolete = armory  # Optional, makes listed building obsolete when this building is constructed
 
   one_per_country = yes
   indestructible = yes               # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = city has_building = metropolis } 
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		army_tradition = 0.1
		land_morale = 0.05
		shock_damage = 0.05
		fire_damage = 0.05
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 80
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
  #  }
}

admiralty = {
	cost = 1500              # Cost of the building in ducats
    time = 180             # Time (in months) that it takes to construct the building
  #  make_obsolete = armory  # Optional, makes listed building obsolete when this building is constructed
 
   one_per_country = yes
   indestructible = yes               # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = city has_building = metropolis } 
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		navy_tradition = 0.1
		naval_morale = 0.05
		naval_attrition = -0.1	
		sailor_maintenance_modifer = -0.1
		ship_durability = 0.05
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 80
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
  #  }
}

stock_exchange = {
	cost = 2000              # Cost of the building in ducats
    time = 120             # Time (in months) that it takes to construct the building
  #  make_obsolete = armory  # Optional, makes listed building obsolete when this building is constructed
 
   one_per_country = yes
   indestructible = yes               # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = city has_building = metropolis } 
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		global_trade_goods_size_modifier = 0.1
		trade_efficiency = 0.1
		merchants = 1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 80
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
  #  }
}

###
infirmary
hospital

toll_house
customs_house

bath
public_sanitation

library
observatory

furnace
refinery

monument
zoo

star_fort
redoubt
colonial_outpost
colonial_fort


