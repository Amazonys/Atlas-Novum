#Miscellanious Flavour Decisions
country_decisions = {

	IR_return_ALE_feudal_lands = {
	
		potential = {
			tag = FRA
			1879 = { owned_by = FRA }
			7983 = { owned_by = FRA }
			NOT = { has_country_flag = ALE_returned_land }
			ALE = { exists = no }
		}
		allow = {
			1879 = { owned_by = FRA }
			7983 = { owned_by = FRA }
			adm_power = 50
		}
		effect = {
			1879 = { cede_province = ALE }
			7980 = { cede_province = ALE }
			add_adm_power = -50
			add_dip_power = 100
			ALE = { add_government_reform = french_principality }
			set_country_flag = ALE_returned_land
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	IR_ned_fund_rebels = {
		potential = {
			low_countries_primary_cultures = yes
			NOT = { tag = NED }
		}
		allow = {
			treasury = 500
		}
		effect = {
			#any nation with territory in low countries region gets indepedence rebels
		}
		ai_will_do = {
			factor = 1
			#if negative income, dont.
		}
	}
	
	IR_request_EFR_recognition = {
		major = yes
		potential = {
			tag = EFR
			has_country_modifier = IR_eastfrisian_chiefs
			NOT = { has_country_flag = Request_EFR_decision }
		}
		allow = {
			prestige = 25
		}
		effect = {
			set_country_flag = Request_EFR_decision
			add_prestige = -25
			emperor = {
				country_event = { 
					id = IR_flavor_misc.5
					days = 0
				}
			}
		}
		ai_will_do = {
			factor = 1
			#if negative income, dont.
		}
	}
	
#END	
}