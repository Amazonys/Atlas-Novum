#Naples missions
#Written by Vixor for Imperialism Reinvigorated 

nap_slot_1 = {
	slot = 1 # leftmost column, currently empty
	generic = no #if a mission without generic = yes wants this slot then it overrides this.
	ai = yes
	potential = {
		tag = NAP
	}
	has_country_shield = yes
	
}

nap_slot_2 = {
	slot = 2 #
	generic = no #if a mission without generic = yes wants this slot then it overrides this.
	ai = yes
	potential = {
		tag = NAP
	}
	has_country_shield = yes
	
}
nap_slot_3 = {
	slot = 3 
	generic = no #if a mission without generic = yes wants this slot then it overrides this.
	ai = yes
	potential = {
		tag = NAP
	}
	has_country_shield = yes
	
}
nap_slot_4 = {
	slot = 4
	generic = no #if a mission without generic = yes wants this slot then it overrides this.
	ai = yes
	potential = {
		tag = NAP
	}
	has_country_shield = yes
	
}
nap_slot_5 = {
	slot = 5 
	generic = no #if a mission without generic = yes wants this slot then it overrides this.
	ai = yes
	potential = {
		tag = NAP
	}
	has_country_shield = yes
	
	
	nap_build_the_navy = {
		icon = mission_galleys_in_port
		required_missions = {}
		
		trigger = {
			navy_size_percentage = 0.85
		}
		
		effect = {
			add_dip_power = 50
			add_navy_tradition = 15
			add_country_modifier = {
				name = "naval_enthusiasm_mission"
				duration = 9125 #25 years
			}
		}
	}
	
	nap_corsica = {
		icon = mission_cannons_firing
		required_missions = { nap_build_the_navy }
		position = 3
		provinces_to_highlight = {
			area = corsica_sardinia_area
			NOT = {
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		trigger = {
			corsica_sardinia_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			sardinia_area = {
				limit = { NOT = { country_or_non_sovereign_subject_holds = ROOT } }
				add_permanent_claim = ROOT
			}
		}
	}
	nap_sardinia = {
		icon = mission_cannons_firing
		required_missions = { tus_corsica }
		position = 4
		provinces_to_highlight = {
			area = sardinia_area 
			NOT = {
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		trigger = {
			sardinia_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			sicily_area = {
				limit = { NOT = { country_or_non_sovereign_subject_holds = ROOT } }
				add_permanent_claim = ROOT
			}
			balearic_islands_area = {
				limit = { NOT = { country_or_non_sovereign_subject_holds = ROOT } }
				add_permanent_claim = ROOT
			}
		}
	}
	nap_sicily = {
		icon = mission_cannons_firing
		required_missions = { nap_sardinia }
		position = 5
		provinces_to_highlight = {
			area = sicily_area 
			NOT = {
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		trigger = {
			sicily_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			341 = {
				limit = { NOT = { country_or_non_sovereign_subject_holds = ROOT } }
				add_permanent_claim = ROOT
			}
		}
	}
}
	