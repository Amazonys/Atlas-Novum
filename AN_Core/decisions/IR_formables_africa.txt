country_decisions = {
	
IR_guanche_nation = {
        major = yes
        potential = {
            NOT = { has_country_flag = formed_guanac_flag }
            capital_scope = { area = canaries_area }
            GNC = { exists = no }
            is_nomad = no
            primary_culture = guanche
        }
        provinces_to_highlight = {
            OR = { 
                area = canaries_area
            }
        }
        allow = {
            canaries_area = { 
                type = all
                country_or_non_sovereign_subject_holds = ROOT 
            }
            is_at_war = no
        }
        effect = {
            swap_non_generic_missions = yes
            every_subject_country = { 
                limit = { capital_scope = { area = canaries_area } }
                ROOT = { inherit = PREV }
            }
            change_tag = GNC            
            if = { limit = { NOT = { government_rank = 2 } }
                set_government_rank = 2
            }
            add_country_modifier = {
                name = centralization_modifier
                duration = 7300
            }
            add_prestige = 25
            #if = { #NO UNIQUE IDEAS YET
            #    limit = {
            #        has_custom_ideas = no
            #        NOT = { has_idea_group = SPA_ideas }
            #    }
            #    country_event = { id = ideagroups.1 } #Swap Ideas
            #}
            set_country_flag = formed_guanac_flag
        }
        ai_will_do = {
            factor = 1
        }
        ai_importance = 400 #
    }
}