# The custom guiButtonType/instantTextBoxType/iconType must have a 'scripted = yes' line to work
custom_icon = {
    name = AN_global_check_1 #Must match a scripted iconType in a .gui file
    potential = { always = yes } #Determines when the icon is visible
    tooltip = CHECK_GLOBAL_TEXT_1 #Can use customizable localisation
    #frame = {  #Can define multiple frame clauses
    #    number = <int> #Which frame to use. Check dds files with multiple icons like icon_religion.dds
    #    trigger = { } #First matched frame clause will be used.
    #}
}

custom_icon = {
    name = AN_global_check_2 #Must match a scripted iconType in a .gui file
    potential = { always = yes } #Determines when the icon is visible
    tooltip = CHECK_GLOBAL_TEXT_2 #Can use customizable localisation
    #frame = {  #Can define multiple frame clauses
    #    number = <int> #Which frame to use. Check dds files with multiple icons like icon_religion.dds
    #    trigger = { } #First matched frame clause will be used.
    #}
}

custom_icon = {
    name = AN_global_check_3 #Must match a scripted iconType in a .gui file
    potential = { always = yes } #Determines when the icon is visible
    tooltip = CHECK_GLOBAL_TEXT_3 #Can use customizable localisation
    #frame = {  #Can define multiple frame clauses
    #    number = <int> #Which frame to use. Check dds files with multiple icons like icon_religion.dds
    #    trigger = { } #First matched frame clause will be used.
    #}
}

custom_icon = {
    name = AN_global_check_4 #Must match a scripted iconType in a .gui file
    potential = { always = yes } #Determines when the icon is visible
    tooltip = CHECK_GLOBAL_TEXT_4 #Can use customizable localisation
    #frame = {  #Can define multiple frame clauses
    #    number = <int> #Which frame to use. Check dds files with multiple icons like icon_religion.dds
    #    trigger = { } #First matched frame clause will be used.
    #}
}

custom_icon = {
    name = AN_price_check_1 #Must match a scripted iconType in a .gui file
    potential = { always = yes } #Determines when the icon is visible
    tooltip = CHECK_PRICES_TEXT_1 #Can use customizable localisation
    #frame = {  #Can define multiple frame clauses
    #    number = <int> #Which frame to use. Check dds files with multiple icons like icon_religion.dds
    #    trigger = { } #First matched frame clause will be used.
    #}
}

custom_icon = {
    name = AN_price_check_2 #Must match a scripted iconType in a .gui file
    potential = { always = yes } #Determines when the icon is visible
    tooltip = CHECK_PRICES_TEXT_2 #Can use customizable localisation
    #frame = {  #Can define multiple frame clauses
    #    number = <int> #Which frame to use. Check dds files with multiple icons like icon_religion.dds
    #    trigger = { } #First matched frame clause will be used.
    #}
}

custom_icon = {
    name = AN_price_check_3 #Must match a scripted iconType in a .gui file
    potential = { always = yes } #Determines when the icon is visible
    tooltip = CHECK_PRICES_TEXT_3 #Can use customizable localisation
    #frame = {  #Can define multiple frame clauses
    #    number = <int> #Which frame to use. Check dds files with multiple icons like icon_religion.dds
    #    trigger = { } #First matched frame clause will be used.
    #}
}

custom_icon = {
    name = AN_price_check_4 #Must match a scripted iconType in a .gui file
    potential = { always = yes } #Determines when the icon is visible
    tooltip = CHECK_PRICES_TEXT_4 #Can use customizable localisation
    #frame = {  #Can define multiple frame clauses
    #    number = <int> #Which frame to use. Check dds files with multiple icons like icon_religion.dds
    #    trigger = { } #First matched frame clause will be used.
    #}
}