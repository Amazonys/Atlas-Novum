#BUIDLINGS OVERHAUL - AMYZONIS
#
# INTERNAL TAG REFERENCE CONVERSION LIST
# Tier 1, 15th Century Buildings
#marketplace
#workshop
#temple - Temple
#barracks
#shipyard
#fort_15th - Castle
#coastal_defence
# Tier 2, 16th Century Buildings
#courthouse
#dock
#regimental_camp
#fort_16th - Keep
#naval_battery
# Tier 3, 17th Century Buildings
#cathedral - Cathedral
#university
#trade_depot
#grand_shipyard
#training_fields
#fort_17th - Bastion
# Tier 4, 18th Century Buildings
#stock_exchange
#counting_house
#town_hall
#drydock
#conscription_center
#fort_18th - Citadel
# Manufactories
#manufactory - Manufactory
#wharf
#weapons
#textile
#plantations - Plantation
#tradecompany
#farm_estate - Farm
#mills - Mill
#furnace
#ramparts
#soldier_households
#impressment_offices
#state_house

#RURAL
manufactory = {
	cost = 50
	time = 30
	modifier = {
		trade_goods_size = 0.5
		trade_goods_size_modifier = 0.2
		
		#local_development_cost = 0.5
	}
}
farm_estate = {
	cost = 50
	time = 30
	manufactory = {
		grain
		rice
		maize
		potatoes
		millet
		herbs
		cassava
		dairy
		legumes
	}
	#onmap = no
	
	build_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	keep_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			OR = { has_building = village has_building = town has_building = city has_building = metropolis }
		}	
	}
}
fishery = {
	cost = 50
	time = 30
	manufactory = {
		fish
		pelagic_fish
		shoal_fish
		whales
		crustaceans
		mollusk
	}
	#onmap = yes
	
	build_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	keep_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			OR = { has_building = village has_building = town has_building = city has_building = metropolis }
		}	
	}
}
orchard = {
	cost = 50
	time = 30
	manufactory = {
		wine
		fruit
		dates
		olives
		tomatoes
	}
	#onmap = yes
	
	build_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	keep_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			OR = { has_building = village has_building = town has_building = city has_building = metropolis }
		}	
	}
}
pasture = {
	cost = 50
	time = 30
	manufactory = {
		wool
		livestock
		camels
		horses
		poultry
	}
	#onmap = yes
	
	build_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	keep_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			OR = { has_building = village has_building = town has_building = city has_building = metropolis }
		}	
	}
}
plantations = {
	cost = 50
	time = 30
	manufactory = {
		black_pepper
		cloves
		coffee
		cotton
		cinnamon
		chili_peppers
		nuts
		sugar
		tobacco
		tea
		cocoa
		palm
		dyes
		opium
		hemp
		saffron
		vanilla
		rubber
	}
	#onmap = yes
	
	build_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	keep_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			OR = { has_building = village has_building = town has_building = city has_building = metropolis }
		}	
	}
}
mine = {
	cost = 50
	time = 30
	manufactory = {
		copper
		gold
		jade
		iron
		salt
		tin
		silver
		zinc
		lead
		sulfur
		#
		coal
		phosphate
	}
	#onmap = yes
	
	build_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	keep_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			OR = { has_building = village has_building = town has_building = city has_building = metropolis }
		}	
	}
}
quarry = {
	cost = 50
	time = 30
	manufactory = {
		alum
		gems
		stone
		marble
		niter # BIG ?
		#
		chromite
	}
	#onmap = yes
	
	build_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	keep_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			OR = { has_building = village has_building = town has_building = city has_building = metropolis }
		}	
	}
}
mills = {
	cost = 50
	time = 30
	manufactory = {
		paper
		softwood
		hardwood
		tropical_wood
	}
	#onmap = yes
	
	build_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	keep_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			OR = { has_building = village has_building = town has_building = city has_building = metropolis }
		}	
	}
}
lodge = {
	cost = 50
	time = 30
	manufactory = {
		birds
		bison
		ivory
		fur
		incense
	}
	#onmap = yes
	
	build_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	keep_trigger = { NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } }
	
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			OR = { has_building = village has_building = town has_building = city has_building = metropolis }
		}	
	}
}	
brewery = {
	cost = 50
	time = 30
	manufactory = {
		apiculture
		beer
	}
	#onmap = yes
	
	build_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	keep_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}	
	}
}
distillery = { 
	cost = 50
	time = 30
	manufactory = {
		liquor
		rum
	}
	#onmap = yes
	
	build_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	keep_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}	
	}

}
yard = {
	cost = 50
	time = 30
	manufactory = {
		fertilizer
		naval_supplies
	}
	#onmap = yes
	
	build_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } 
	
	keep_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } } 
	
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}	
	}
}
guild = {
	cost = 50
	time = 30
	manufactory = {
		artifices
		leather
		confectionary
		furniture
		glass
		jewelry
		chinaware
	}
	
	build_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	keep_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}	
	}
}
printing_press = { 
	cost = 50
	time = 30
	manufactory = {
		books
	}
	
	build_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	keep_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}	
	}
}
assembly = {
	cost = 50
	time = 30
	manufactory = {
		machines
		machine_parts
		preserves
	}
	
	build_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	keep_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}	
	}
}
refinery = {
	cost = 50
	time = 30
	manufactory = {
		chemicals
		concrete
		medicine
		perfume
	}
	
	build_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	keep_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	#onmap = yes

	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}	
	}
}
weapons = { 
	cost = 50
	time = 30
	manufactory = {
		munitions
		gunpowder
		explosives
	}
	
	build_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	keep_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	#onmap = yes

	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}	
	}
}
textile = {
	cost = 50
	time = 30
	manufactory = {
		wool
		cloth
		silk
		carpet
	}
	
	build_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	keep_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	#onmap = yes

	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}	
	}
}
tradecompany = {
	cost = 50
	time = 30
	manufactory = {
		slaves
		spices
		resin
	}
	
	build_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	keep_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	#onmap = yes

	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}	
	}
}
furnace = {
	cost = 50
	time = 30
	manufactory = {
		mercury #extracted from  ore
		brass
		bronze
		metalwork
		steel
		aluminum
	}
	
	build_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	keep_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	#potential = {
	#	has_dlc = "Rule Britannia"
	#}

	#modifier = {
	#	global_trade_goods_size_modifier = 0.05 #National Bonus
	#}
	#onmap = yes
	#show_separate = yes # Means as a manufactory it will be shown according to the old way

	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}	
	}
}
kiln = {
	cost = 50
	time = 30
	manufactory = {
		pottery 
		bricks
	}
	
	build_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	keep_trigger = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
	
	#potential = {
	#	has_dlc = "Rule Britannia"
	#}

	#modifier = {
	#	global_trade_goods_size_modifier = 0.05 #National Bonus
	#}
	#onmap = yes
	#show_separate = yes # Means as a manufactory it will be shown according to the old way

	ai_will_do = {
		factor = 9999
		modifier = {
			factor = 0
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}	
	}
}


#
castle = { #fort_15th
	cost = 50
	time = 18
	
	modifier = {
		fort_level = 1
		local_unrest = -1
	}
	
	build_trigger = {
		NOT = { OR = { has_building = star_fort has_building = colonial_fort } }
	}
	
	#onmap = yes
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

keep = { #fort_16th
	cost = 100
	time = 36
	make_obsolete = castle
	
	modifier = {
		fort_level = 2
		local_unrest = -2
	}
	
	build_trigger = {
		NOT = { OR = { has_building = star_fort has_building = colonial_fort } }
	}

	#onmap = yes	
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

bastion = { #fort_17th
	cost = 200
	time = 72	
	make_obsolete = keep
	
	modifier = {
		fort_level = 3
		local_unrest = -3
	}
	
	build_trigger = {
		NOT = { OR = { has_building = star_fort has_building = colonial_fort } }
	}

	#onmap = yes	
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

citadel = { #citadel
	cost = 400
	time = 144	
	make_obsolete = bastion
	
	modifier = {
		fort_level = 4
		local_unrest = -4
	}
	
	build_trigger = {
		NOT = { OR = { has_building = star_fort has_building = colonial_fort } }
	}

	#onmap = yes
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

#
shrine = {
	cost = 50
	time = 18	
	
	modifier = {
		local_missionary_strength  = 0.01
		local_tax_modifier = 0.05
		#local_governing_cost = -0.01
	}

	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

monastery = {
	cost = 100
	time = 36	
	make_obsolete = shrine
	modifier = {
		local_missionary_strength  = 0.015
		local_tax_modifier = 0.1
	#	local_governing_cost = -0.02
	}

	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

temple = {
	cost = 200
	time = 72	
	make_obsolete = monastery	
	modifier = {
		local_missionary_strength  = 0.02
		local_tax_modifier = 0.15
	#	local_governing_cost = -0.03
	}
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

cathedral = {
	cost = 400
	time = 144	
	make_obsolete = temple	
	modifier = {
		local_missionary_strength  = 0.03
		local_tax_modifier = 0.2
	#	local_governing_cost = -0.05
	}
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

# URBAN
village = {
	cost = 100               # Cost of the building in ducats
    time = 12             # Time (in months) that it takes to construct the building
  
    indestructible = yes            # Optional, exempts this building from being destroyed via bankruptcy
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		base_manpower = 5
		#has_province_modifier = is_rural
    }
	
	keep_trigger = {
		base_manpower = 5
		#NOT = { has_province_modifier = is_rural }
	}
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
        local_development_cost = 0.05
		supply_limit_modifier = 0.1
		local_institution_spread = 0.05
		allowed_num_of_buildings = 2
		allowed_num_of_manufactories = -1
		#local_governing_cost = 0.25
		statewide_governing_cost = 0.1
	}
	
	#on_built = {
	#}
	
	#on_destroyed = {
		
	#}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 9999
		modifier = {
			factor = 1
			base_manpower = 5  
		}
		modifier = {
			factor = 0 
			NOT = { base_manpower = 5 }
		}
		modifier = {
			factor = 0 
			NOT = { OR = { trade_goods = grain trade_goods = rice trade_goods = maize trade_goods = potatoes trade_goods = millet trade_goods = herbs trade_goods = cassava trade_goods = dairy trade_goods = legumes trade_goods = fish trade_goods = pelagic_fish trade_goods = shoal_fish trade_goods = whales trade_goods = crustaceans trade_goods = mollusk trade_goods = wine trade_goods = fruit trade_goods = dates trade_goods = olives trade_goods = tomatoes trade_goods = wool trade_goods = livestock trade_goods = camels trade_goods = horses trade_goods = poultry trade_goods = black_pepper trade_goods = cloves trade_goods = coffee trade_goods = cotton trade_goods = cinnamon trade_goods = chili_peppers trade_goods = nuts trade_goods = sugar trade_goods = tobacco trade_goods = tea trade_goods = cocoa trade_goods = palm trade_goods = dyes trade_goods = opium trade_goods = hemp trade_goods = saffron trade_goods = vanilla trade_goods = rubber trade_goods = copper trade_goods = gold trade_goods = jade trade_goods = iron trade_goods = salt trade_goods = tin trade_goods = silver trade_goods = zinc trade_goods = lead trade_goods = sulfur trade_goods = coal trade_goods = phosphate trade_goods = alum trade_goods = gems trade_goods = stone trade_goods = marble trade_goods = niter trade_goods = chromite trade_goods = paper trade_goods = softwood trade_goods = hardwood trade_goods = tropical_wood trade_goods = birds trade_goods = bison trade_goods = ivory trade_goods = fur trade_goods = incense } }
		}
	} 
}
town = {
	cost = 200               # Cost of the building in ducats
    time = 12             # Time (in months) that it takes to construct the building
    make_obsolete = village  # Optional, makes listed building obsolete when this building is constructed
  
    indestructible = yes            # Optional, exempts this building from being destroyed via bankruptcy
   
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		base_manpower = 10
		#NOT = { has_province_modifier = is_rural }
    }
	
	keep_trigger = {
		base_manpower = 10
		#NOT = { has_province_modifier = is_rural }
	}
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
        local_development_cost = 0.1
		supply_limit_modifier = 0.2
		local_institution_spread = 0.1
		allowed_num_of_buildings = 3		
		allowed_num_of_manufactories = -1
		#local_governing_cost = 0.5
		statewide_governing_cost = 0.2
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 9999
		modifier = {
			factor = 1
			base_manpower = 10
		}
		modifier = {
			factor = 0 
			NOT = { base_manpower = 10 }
		}
    }
}
city = {
	cost = 300               # Cost of the building in ducats
    time = 12 # Time (in months) that it takes to construct the building
    make_obsolete = town  # Optional, makes listed building obsolete when this building is constructed
  
    indestructible = yes            # Optional, exempts this building from being destroyed via bankruptcy
  
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		base_manpower = 25
		#NOT = { has_province_modifier = is_rural }
    }
	
	keep_trigger = {
		base_manpower = 25
		#NOT = { has_province_modifier = is_rural }
	}
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
        local_development_cost = 0.15
		supply_limit_modifier = 0.3
		local_institution_spread = 0.15
		allowed_num_of_buildings = 4		
		allowed_num_of_manufactories = -1
		#local_governing_cost = 0.75
		statewide_governing_cost = 0.3
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 9999
		modifier = {
			factor = 1
			base_manpower = 25
		}
		modifier = {
			factor = 0 
			NOT = { base_manpower = 25 }
		}
    }
}
metropolis = { 
	cost = 400               # Cost of the building in ducats
    time = 12           # Time (in months) that it takes to construct the building
    make_obsolete = city  # Optional, makes listed building obsolete when this building is constructed
  
	indestructible = yes            # Optional, exempts this building from being destroyed via bankruptcy
 
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		base_manpower = 50
	#	NOT = { has_province_modifier = is_rural }
    }
	
	keep_trigger = {
		base_manpower = 50
		#NOT = { has_province_modifier = is_rural }
	}
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
        local_development_cost = 0.2
		supply_limit_modifier = 0.4
		local_institution_spread = 0.2
		allowed_num_of_buildings = 5		
		allowed_num_of_manufactories = -1
		#local_governing_cost = 1
		statewide_governing_cost = 0.4
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 9999 
		modifier = {
			factor = 1
			base_manpower = 50 
		}
		modifier = {
			factor = 0 
			NOT = { base_manpower = 50 }
		}
    }
}

#
marketplace = {
	cost = 75               # Cost of the building in ducats
    time = 12             # Time (in months) that it takes to construct the building
   # make_obsolete = <building>  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 1 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		province_trade_power_value = 1
		local_production_efficiency = 0.1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis province_has_center_of_trade_of_level = 1 } }
		}
    }
}
trade_depot = {
	cost = 150               # Cost of the building in ducats
    time = 24             # Time (in months) that it takes to construct the building
    make_obsolete = marketplace  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 2 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		province_trade_power_value = 2.5
		local_production_efficiency = 0.2
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis province_has_center_of_trade_of_level = 2 } }
		}
    }
}
counting_house = {
	cost = 300               # Cost of the building in ducats
    time = 36             # Time (in months) that it takes to construct the building
    make_obsolete = trade_depot  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 3 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		province_trade_power_value = 5
		local_production_efficiency = 0.3
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis province_has_center_of_trade_of_level = 3 } }
		}
    }
}
bank = {
	cost = 500                 # Cost of the building in ducats
    time = 60             # Time (in months) that it takes to construct the building
    make_obsolete = counting_house  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 4 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		province_trade_power_value = 5
		local_production_efficiency = 0.5
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis province_has_center_of_trade_of_level = 4 } }
		}
    }
}

#
jetty = {
	cost = 100               # Cost of the building in ducats
    time = 12             # Time (in months) that it takes to construct the building
   # make_obsolete = <building>  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		has_port = yes
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 1 
		}
    }
	keep_trigger = {
        OR = { 
            has_building = village 
            has_building = town 
            has_building = city
            has_building = metropolis
        }
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_sailors_modifier  = 0.25
		province_trade_power_modifier = 0.1
		naval_forcelimit = 1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis province_has_center_of_trade_of_level = 1 } }
		}
    }
}
wharf = {
	cost = 200               # Cost of the building in ducats
    time = 24             # Time (in months) that it takes to construct the building
    make_obsolete = jetty  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		has_port = yes
		OR = { 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 2 
		}
    }
	keep_trigger = {
        OR = { 
            has_building = village 
            has_building = town 
            has_building = city
            has_building = metropolis
        }
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_sailors_modifier  = 0.5
		province_trade_power_modifier = 0.2
		naval_forcelimit = 2
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis province_has_center_of_trade_of_level = 2 } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
    on_built = {
		THIS = { 
			owner = {
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = 2
				#}
			}
		}
    }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
    on_destroyed = {
		THIS = { 
			owner = { 
				#change_estate_land_share = {
				#	estate = estate_burghers
				#	share = -2
				#}
				#add_estate_loyalty = {
				#	estate = estate_burghers
				#	loyalty = -10
				#}
			}
		}
    }
}
dock = { #dockyard
	cost = 400               # Cost of the building in ducats
    time = 36             # Time (in months) that it takes to construct the building
    make_obsolete = wharf  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		has_port = yes
		OR = { 
			has_building = city 
			has_building = metropolis 
			province_has_center_of_trade_of_level = 3 
		}
    }
	keep_trigger = {
        OR = { 
            has_building = village 
            has_building = town 
            has_building = city
            has_building = metropolis
        }
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_sailors_modifier  = 0.75
		province_trade_power_modifier = 0.4
		naval_forcelimit = 4
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis province_has_center_of_trade_of_level = 3 } }
		}
    }
}
impressment_offices = { #harbor
	cost = 800               # Cost of the building in ducats
    time = 60             # Time (in months) that it takes to construct the building
    make_obsolete = dock  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		has_port = yes
		OR = {
			has_building = metropolis 
			province_has_center_of_trade_of_level = 4 
		}
    }
	keep_trigger = {
        OR = { 
            has_building = village 
            has_building = town 
            has_building = city
            has_building = metropolis
        }
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_sailors_modifier  = 1
		province_trade_power_modifier = 0.6
		local_institution_spread = 0.05
		naval_forcelimit = 8
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = metropolis province_has_center_of_trade_of_level = 4 } }
		}
    }
}

#
school = {
	cost = 100               # Cost of the building in ducats
    time = 24             # Time (in months) that it takes to construct the building
   # make_obsolete = <building>  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
		}
    }
	
	keep_trigger = {
		has_building = school
	}
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_institution_spread = 0.15
		innovativeness_gain = 0.01
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}
    }
}
college = {
	cost = 200               # Cost of the building in ducats
    time = 48             # Time (in months) that it takes to construct the building
    make_obsolete = school  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = town 
			has_building = city 
			has_building = metropolis 
		}
    }
	
	keep_trigger = {
		has_building = college
	}
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_institution_spread = 0.25
		innovativeness_gain = 0.015
		local_build_cost = -0.05
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis  } }
		}
    }
}
university = {
	cost = 400               # Cost of the building in ducats
    time = 72             # Time (in months) that it takes to construct the building
    make_obsolete = college  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = city 
			has_building = metropolis 
		}
    }
	
	keep_trigger = {
		has_building = university
	}
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_institution_spread = 0.5
		innovativeness_gain = 0.025
		local_build_cost = -0.1
		statewide_governing_cost = -0.05
		local_unrest = 0.5
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
}
academy = {
	cost = 800               # Cost of the building in ducats
    time = 120             # Time (in months) that it takes to construct the building
    make_obsolete = university  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		#OR = { 
			#has_building = city 
			has_building = metropolis 
		#}
    }
	
	keep_trigger = {
		has_building = academy
	}
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		local_institution_spread = 1
		innovativeness_gain = 0.05
		local_build_cost = -0.15
		statewide_governing_cost = -0.1
		local_unrest = 1
		army_tradition = 0.01
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { has_building = metropolis } 
		}
    }
}

#
cottage = {
	cost = 100               # Cost of the building in ducats
    time = 12             # Time (in months) that it takes to construct the building
   # make_obsolete = <building>  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			#has_province_modifier = is_rural
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_goods_size = 0.25
		trade_goods_size_modifier = 0.05
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}
    }
}
workshop = {
	cost = 250               # Cost of the building in ducats
    time = 24             # Time (in months) that it takes to construct the building
    make_obsolete = cottage  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = town 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_goods_size = 0.5
		trade_goods_size_modifier = 0.1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis  } }
		}
    }
}
new_manufactory = {
	cost = 500               # Cost of the building in ducats
    time = 36             # Time (in months) that it takes to construct the building
    make_obsolete = workshop  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_goods_size = 1
		trade_goods_size_modifier = 0.15		
		allowed_num_of_manufactories = 1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
}
factory = {
	cost = 1000               # Cost of the building in ducats
    time = 60             # Time (in months) that it takes to construct the building
    make_obsolete = new_manufactory  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		#OR = { 
		#	has_building = city 
			has_building = metropolis 
		#}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_goods_size = 2
		trade_goods_size_modifier = 0.2		
		allowed_num_of_manufactories = 1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { has_building = metropolis }
		}
    }
}

#
studio = {
	cost = 150               # Cost of the building in ducats
    time = 12             # Time (in months) that it takes to construct the building
   # make_obsolete = <building>  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_value_modifier = 0.1
		monthly_splendor = 0.25
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = village has_building = town has_building = city has_building = metropolis } }
		}
    }
}
artisanate = {
	cost = 300               # Cost of the building in ducats
    time = 24             # Time (in months) that it takes to construct the building
    make_obsolete = studio  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = town 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_value_modifier = 0.15
		monthly_splendor = 0.5
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis  } }
		}
    }
}
theatre = {
	cost = 500               # Cost of the building in ducats
    time = 36             # Time (in months) that it takes to construct the building
    make_obsolete = artisanate  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { 
			has_building = city 
			has_building = metropolis 
		}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_value_modifier = 0.2
		monthly_splendor = 1
		local_unrest = -0.5
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = city has_building = metropolis } }
		}
    }
}
museum = {
	cost = 1200               # Cost of the building in ducats
    time = 60             # Time (in months) that it takes to construct the building
    make_obsolete = theatre  # Optional, makes listed building obsolete when this building is constructed
  
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		#OR = {
		#	has_building = city
			has_building = metropolis 
		#}
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_value_modifier = 0.25
		monthly_splendor = 2
		local_unrest = -1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { has_building = metropolis }
		}
    }
}

#ELSE
fort_15th = { #palisade
	cost = 50
	time = 18
	modifier = {
		fort_level = 1
	}
	
	build_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			#
			has_building = castle
			has_building = keep
			has_building = bastion
			has_building = citadel
			#
			has_building = shrine
			has_building = monastery
			has_building = temple
			has_building = cathedral
		}
		NOT = { has_building = star_fort }
    }
	keep_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			#
			has_building = castle
			has_building = keep
			has_building = bastion
			has_building = citadel
			#
			has_building = shrine
			has_building = monastery
			has_building = temple
			has_building = cathedral
		}
		NOT = { has_building = star_fort }
	}
	
	onmap = yes
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
fort_16th = { #walls
	cost = 100
	time = 36
	make_obsolete = fort_15th
	modifier = {
		fort_level = 2
	}
	
	build_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			#
			has_building = castle
			has_building = keep
			has_building = bastion
			has_building = citadel
			#
			has_building = shrine
			has_building = monastery
			has_building = temple
			has_building = cathedral
		}
		NOT = { has_building = star_fort }
    }
	keep_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			#
			has_building = castle
			has_building = keep
			has_building = bastion
			has_building = citadel
			#
			has_building = shrine
			has_building = monastery
			has_building = temple
			has_building = cathedral
		}
		NOT = { has_building = star_fort }
	}
	onmap = yes
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
fort_17th = { #ramparts
	cost = 200
	time = 72
	make_obsolete = fort_16th
	modifier = {
		fort_level = 3
	}
	build_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			#
			has_building = castle
			has_building = keep
			has_building = bastion
			has_building = citadel
			#
			has_building = shrine
			has_building = monastery
			has_building = temple
			has_building = cathedral
		}
		NOT = { has_building = star_fort }
    }
	keep_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			#
			has_building = castle
			has_building = keep
			has_building = bastion
			has_building = citadel
			#
			has_building = shrine
			has_building = monastery
			has_building = temple
			has_building = cathedral
		}
		NOT = { has_building = star_fort }
	}
	onmap = yes
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
fort_18th = { #battlements
	cost = 400
	time = 144
	make_obsolete = fort_17th
	modifier = {
		fort_level = 4
	}
	build_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			#
			has_building = castle
			has_building = keep
			has_building = bastion
			has_building = citadel
			#
			has_building = shrine
			has_building = monastery
			has_building = temple
			has_building = cathedral
		}
		NOT = { has_building = star_fort }
    }
	keep_trigger = {
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city 
			has_building = metropolis 
			#
			has_building = castle
			has_building = keep
			has_building = bastion
			has_building = citadel
			#
			has_building = shrine
			has_building = monastery
			has_building = temple
			has_building = cathedral
		}
		NOT = { has_building = star_fort }
	}
	onmap = yes
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

watchtowers = {
	cost = 50
	time = 18
	modifier = {
		local_defensiveness = 0.1
		local_hostile_movement_speed = -0.15
	}
	
	keep_trigger = {
		has_building = watchtowers
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
barbican = {
	cost = 100
	time = 36
	modifier = {
		local_defensiveness = 0.2
		garrison_growth = 0.25
	}
	build_trigger = {
		OR = { 
		#	has_building = fort_15th 
			has_building = fort_16th 
			has_building = fort_17th
			has_building = fort_18th
			has_building = colonial_fort
		}
    }
	keep_trigger = {
		OR = { 
		#	has_building = fort_15th 
			has_building = fort_16th 
			has_building = fort_17th
			has_building = fort_18th
			has_building = colonial_fort
		}
    }	
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
ravelins = {
	cost = 200
	time = 72
	modifier = {
		local_defensiveness = 0.5
		local_hostile_attrition = 2
	}
	build_trigger = {
		OR = { 
		#	has_building = fort_15th 
		#	has_building = fort_16th 
			has_building = fort_17th
			has_building = fort_18th
			has_building = colonial_fort
		}
    }
	keep_trigger = {
		OR = { 
		#	has_building = fort_15th 
		#	has_building = fort_16th 
			has_building = fort_17th
			has_building = fort_18th
			has_building = colonial_fort
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
battery = {
	cost = 400
	time = 144
	modifier = {
		local_hostile_attrition = 1	
		hostile_fleet_attrition = 2
		blockade_force_required = 1
		hostile_disembark_speed = 2
	}
	build_trigger = {
		OR = { 
		#	has_building = fort_15th 
		#	has_building = fort_16th 
		#	has_building = fort_17th
			has_building = fort_18th
			has_building = colonial_fort
		}
    }
	keep_trigger = {
		OR = { 
		#	has_building = fort_15th 
		#	has_building = fort_16th 
		#	has_building = fort_17th
			has_building = fort_18th
			has_building = colonial_fort
		}
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

colonial_fort = {
	cost = 50
	time = 12
	
	build_trigger = {
		is_overseas = yes
		NOT = { 
			OR = {
				has_building = castle
				has_building = keep
				has_building = bastion
				has_building = citadel
			}
		}
	}
	
	
	modifier = {
		fort_level = 1
		local_defensiveness = 0.15
		supply_limit_modifier = 0.25
		province_trade_power_modifier = 0.25
		allowed_num_of_buildings = 1
	}
	
	influencing_fort = yes
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

#
regimental_camp = { #encampment
	cost = 100
	time = 6
	modifier = {
		local_manpower_modifier  = 0.25
		land_forcelimit = 1
		local_regiment_cost = -0.05
		regiment_recruit_speed = -0.1
	}
	build_trigger = {
		#base_tax = 5
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		has_building = regimental_camp
    }

	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
barracks = {
	cost = 200
	time = 18
	make_obsolete = regimental_camp
	modifier = {
		local_manpower_modifier  = 0.5
		land_forcelimit = 2
		local_regiment_cost = -0.1
		regiment_recruit_speed = -0.2
		local_defensiveness = 0.05
	}
	build_trigger = {
		OR = { 
			#has_building = village 
			has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		has_building = barracks
    }
	
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
conscription_center = { #armory
	cost = 400
	time = 60
	make_obsolete = barracks
	modifier = {
		local_manpower_modifier = 0.75
		land_forcelimit = 3
		local_regiment_cost = -0.2
		regiment_recruit_speed = -0.3
		local_defensiveness = 0.1
		local_hostile_attrition = 0.5
	}
	build_trigger = {
		OR = { 
			#has_building = village 
			#has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		has_building = conscription_center
    }

	
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
soldier_households = { #magazine
	cost = 800
	time = 120
	make_obsolete = conscription_center
	modifier = {
		local_manpower_modifier = 1
		land_forcelimit = 6
		local_regiment_cost = -0.4
		regiment_recruit_speed = -0.4
		local_defensiveness = 0.2
		local_hostile_attrition = 1
		
	}
	build_trigger = {
		#OR = { 
			#has_building = village 
			#has_building = town 
			#has_building = city
			has_building = metropolis
		#}
    }
	keep_trigger = {
		has_building = soldier_households
    }
	
	
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}

shipwright = {
	cost = 75
	time = 6
	modifier = {
		local_sailors_modifier  = 0.1
		local_ship_repair = 0.1
		local_ship_cost = -0.05
		ship_recruit_speed = -1
	}
	build_trigger = {
		has_port = yes
		OR = { 
			has_building = village 
			has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		has_building = shipwright
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
shipyard = {
	cost = 150
	time = 18
	make_obsolete = shipwright
	modifier = {
		local_sailors_modifier = 0.25
		local_ship_repair = 0.25
		local_ship_cost = -0.1
		ship_recruit_speed = -1.5
	}
	build_trigger = {
		has_port = yes
		OR = { 
			#has_building = village 
			has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		has_building = shipyard
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
drydock = {
	cost = 300
	time = 60
	make_obsolete = shipyard
	modifier = {
		local_sailors_modifier = 0.5
		local_ship_repair = 0.5
		local_ship_cost = -0.15
		ship_recruit_speed = -2
	}
	build_trigger = {
		has_port = yes
		OR = { 
			#has_building = village 
			#has_building = town 
			has_building = city
			has_building = metropolis
		}
    }
	keep_trigger = {
		has_building = drydock
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
grand_shipyard = { #arsenal
	cost = 600
	time = 120
	make_obsolete = drydock
	modifier = {
		local_sailors_modifier = 1
		local_ship_repair = 1
		local_ship_cost = -0.25
		ship_recruit_speed = -2.5
	}
	build_trigger = {
		has_port = yes
		#OR = { 
			#has_building = village 
			#has_building = town 
			#has_building = city
			has_building = metropolis
		#}
    }
	keep_trigger = {
		has_building = grand_shipyard
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	} 
}
###

#SPECIAL UNIQUE
customs_house = { 
	cost = 250               # Cost of the building in ducats
    time = 24           # Time (in months) that it takes to construct the building
 #   make_obsolete = regimental_camp  # Optional, makes listed building obsolete when this building is constructed
  
  # indestructible = yes            # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = {
			is_overseas = yes
			is_island = yes
		}
		is_state = yes
		NOT = { area_for_scope_province = { has_building = customs_house } }
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_value_modifier = 0.25
		province_trade_power_value = 2
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 1
		modifier = {
			factor = 0 
			is_overseas = no
		}
    }
}

library = {
	cost = 250               # Cost of the building in ducats
    time = 24           # Time (in months) that it takes to construct the building
	
	modifier = {
		local_institution_spread = 1
		innovativeness_gain = 0.01
		local_culture_conversion_cost = -0.1
		local_unrest = -1
	}
	build_trigger = {
		OR = { has_building = town has_building = city has_building = metropolis }
		NOT = { area_for_scope_province = { has_building = library } }
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	}
}

prison = {
	cost = 250               # Cost of the building in ducats
    time = 24           # Time (in months) that it takes to construct the building
	modifier = {
		statewide_governing_cost = -0.1
		local_state_maintenance_modifier = 0.1
		local_unrest = -10
	}
	build_trigger = {
		OR = { has_building = town has_building = city has_building = metropolis }
		NOT = { area_for_scope_province = { has_building = prison } }
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	}
}



#VILLAGE
training_fields = { #bath
	cost = 250               # Cost of the building in ducats
    time = 12           # Time (in months) that it takes to construct the building
	modifier = {
		#statewide_governing_cost = -0.1
		local_state_maintenance_modifier = 0.05
		local_monthly_devastation = -0.05
		#local_unrest = -10
	}
	build_trigger = {
		OR = { has_building = village has_building = town has_building = city has_building = metropolis }
		#NOT = { area_for_scope_province = { has_building = prison } }
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	}
}
constabulary = {
	cost = 250               # Cost of the building in ducats
    time = 12           # Time (in months) that it takes to construct the building
	modifier = {
		local_autonomy = -0.05
		local_state_maintenance_modifier = -0.05
		local_unrest = -2.5
	}
	build_trigger = {
		OR = { has_building = village has_building = town has_building = city has_building = metropolis }
		NOT = { area_for_scope_province = { has_building = constabulary } }
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	}
}

#TOWN
infirmary = {
	cost = 500               # Cost of the building in ducats
    time = 24           # Time (in months) that it takes to construct the building
	make_obsolete = training_fields
	modifier = {
		#statewide_governing_cost = -0.1
		local_state_maintenance_modifier = 0.1
		#local_unrest = -10
		local_monthly_devastation = -0.1
	}
	build_trigger = {
		OR = { has_building = town has_building = city has_building = metropolis }
		#NOT = { area_for_scope_province = { has_building = prison } }
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	}
}
courthouse = {
	cost = 500               # Cost of the building in ducats
    time = 24           # Time (in months) that it takes to construct the building
	make_obsolete = constabulary
	modifier = {
		local_autonomy = -0.1
		local_state_maintenance_modifier = -0.1
		local_unrest = -5
	}
	build_trigger = {
		OR = { has_building = town has_building = city has_building = metropolis }
		NOT = { area_for_scope_province = { has_building = courthouse } }
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	}
}

#CITY
hospital = {
	cost = 750               # Cost of the building in ducats
    time = 36           # Time (in months) that it takes to construct the building
	make_obsolete = infirmary
	modifier = {
		#statewide_governing_cost = -0.1
		local_state_maintenance_modifier = 0.15
		#local_unrest = -10
		local_monthly_devastation = -0.15
	}
	build_trigger = {
		OR = {  has_building = city has_building = metropolis }
		#NOT = { area_for_scope_province = { has_building = prison } }
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	}
}

town_hall = { #city hall
	cost = 750               # Cost of the building in ducats
    time = 36           # Time (in months) that it takes to construct the building
	make_obsolete = courthouse
	modifier = {
		local_autonomy = -0.15
		local_state_maintenance_modifier = -0.15
		local_unrest = -7.5
	}
	build_trigger = {
		OR = { has_building = city has_building = metropolis }
		NOT = { area_for_scope_province = { has_building = town_hall } }
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	}
}


#METRO
public_sanitation = {
	cost = 1000               # Cost of the building in ducats
    time = 60           # Time (in months) that it takes to construct the building
	make_obsolete = hospital
	modifier = {
		#statewide_governing_cost = -0.1
		local_state_maintenance_modifier = 0.2
		local_monthly_devastation = -0.2
		#local_unrest = -10
	}
	build_trigger = {
		has_building = metropolis 
		#NOT = { area_for_scope_province = { has_building = prison } }
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	}
}

state_house = { #public office
	cost = 1000               # Cost of the building in ducats
    time = 60           # Time (in months) that it takes to construct the building
	make_obsolete = town_hall
	modifier = {
		local_autonomy = -0.2
		local_state_maintenance_modifier = -0.2
		local_unrest = -10
	}
	build_trigger = {
		has_building = metropolis
		NOT = { area_for_scope_province = { has_building = state_house } }
    }
	
	ai_will_do = {
		factor = 1 #Lots of logic in code instead, that multiplies with this value
	}
}

## TAG UNIQUE
postmaster_general = { 
	cost = 1000               # Cost of the building in ducats
    time = 120           # Time (in months) that it takes to construct the building
   # make_obsolete = barracks  # Optional, makes listed building obsolete when this building is constructed
 
   one_per_country = yes
   indestructible = yes             # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = town has_building = city has_building = metropolis }
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		trade_range_modifier = 0.5
		envoy_travel_time = -0.5	
		administrative_efficiency = 0.05
		trade_range_modifier = 0.5
		min_autonomy = -5
		min_autonomy_in_territories = -5
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
  #  }
}

national_court = {
	cost = 1000               # Cost of the building in ducats
    time = 120           # Time (in months) that it takes to construct the building
   # make_obsolete = <building>  # Optional, makes listed building obsolete when this building is constructed
  
   one_per_country = yes
   indestructible = yes             # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = town has_building = city has_building = metropolis } 
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		prestige = 0.5
		all_estate_loyalty_equilibrium = 0.5
		governing_capacity = 500
		num_accepted_cultures  = 2
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
	#}
}

national_mint = {
	cost = 1000              # Cost of the building in ducats
    time = 120        # Time (in months) that it takes to construct the building
   # make_obsolete = training_fields  # Optional, makes listed building obsolete when this building is constructed
  
   one_per_country = yes
   indestructible = yes       # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = town has_building = city has_building = metropolis }
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		production_efficiency = 0.1
		inflation_action_cost = -0.5	
		trade_efficiency = 0.05
		inflation_reduction = 0.05
		global_tariffs = 0.1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
  #  }
}

foreign_office = { 
	cost = 1000               # Cost of the building in ducats
    time = 120           # Time (in months) that it takes to construct the building
    #make_obsolete = regimental_camp  # Optional, makes listed building obsolete when this building is constructed
 
   one_per_country = yes
   indestructible = yes            # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = town has_building = city has_building = metropolis } 
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		diplomatic_reputation = 1
		diplomatic_upkeep = 1
		improve_relation_modifier = 0.2
		rebel_support_efficiency = 0.5
		diplomatic_annexation_cost = -0.1
		spy_offence = 0.1
		global_spy_defence = 0.1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 40
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
  #  }
}

war_college = {
	cost = 2000              # Cost of the building in ducats
    time = 180             # Time (in months) that it takes to construct the building
  #  make_obsolete = armory  # Optional, makes listed building obsolete when this building is constructed
 
   one_per_country = yes
   indestructible = yes               # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = town has_building = city has_building = metropolis } 
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		army_tradition = 0.25
		yearly_army_professionalism = 0.01
		#military_tactics = 0.25 #CANNOT BE USED, EXCL TO MIL TECH FILE BC BRUH
		artillery_bonus_vs_fort = 1
		free_leader_pool = 1
		drill_decay_modifier = -0.5
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 80
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
  #  }
}

admiralty = {
	cost = 2000              # Cost of the building in ducats
    time = 180             # Time (in months) that it takes to construct the building
  #  make_obsolete = armory  # Optional, makes listed building obsolete when this building is constructed
 
   one_per_country = yes
   indestructible = yes               # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = town has_building = city has_building = metropolis } 
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		navy_tradition = 0.25
		naval_attrition = -0.1	
		global_ship_repair = 0.2
		blockade_efficiency = 0.25
		siege_blockade_progress = 1
		sailor_maintenance_modifer = -0.1
		free_leader_pool = 1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 80
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
  #  }
}

stock_exchange = {
	cost = 2000              # Cost of the building in ducats
    time = 120             # Time (in months) that it takes to construct the building
  #  make_obsolete = armory  # Optional, makes listed building obsolete when this building is constructed
 
   one_per_country = yes
   indestructible = yes               # Optional, exempts this building from being destroyed via bankruptcy
   #influencing_fort = yes          # Optional, whether this building operates like a fort
    
    # Optional. Provinces with the listed tradegoods can construct this building.
	#manufactory = {
    #    <tradegoods>
    #}
    
    # Optional. Province scope. Limits construction of building if not met.
    build_trigger = {
		OR = { has_building = town has_building = city has_building = metropolis } 
    }
    
    # The modifier the building applies to the province.
    # Country-scope modifiers used here will apply to the owning country of the building instead.
    modifier = {
		global_trade_goods_size_modifier = 0.1
		trade_efficiency = 0.1
		global_trade_power = 0.1
		merchants = 1
	}
    
    # The importance the AI places on constructing this building.
    ai_will_do = {
        factor = 80
		modifier = {
			factor = 0 
			NOT = { OR = { has_building = town has_building = city has_building = metropolis } }
		}
    }
    
   #  Optional. On action that fires when built. 
   #  THIS = Province, FROM = Builder (not necessarily owner, can be overlord)
   # on_built = {
    
  #  }
    
    # Optional. On action that fires when destroyed. (not obsoleted)
    # THIS = Province, FROM = Destroyer
  #  on_destroyed = {
   # 
  #  }
}